name: my-project-check
on: push
jobs:
  # linter chek
  make-lint:
    runs-on: ubuntu-latest
    steps:
      - name: flake8 check
      - uses: actions/checkout@v2
      - run: poetry run flake8 brain_games
      
          

	
